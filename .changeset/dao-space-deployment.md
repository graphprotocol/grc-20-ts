---
"@graphprotocol/grc-20": minor
---

Add DAO space deployment and registration encoding utilities

- Add `DaoSpaceFactoryAbi` for the `createDAOSpaceProxy` function
- Add `SpaceRegistryAbi` for space registration
- Add `getCreateDaoSpaceCalldata` encoding function with voting settings helpers
- Add `getRegisterSpaceCalldata` encoding function
- Add `DAO_SPACE_FACTORY_ADDRESS` and `SPACE_REGISTRY_ADDRESS` to testnet contracts
